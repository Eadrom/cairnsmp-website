# Branding assets

This directory contains web-sized source artwork that Astro processes during builds.

- `cairnsmp-logo.png` — detailed Cairn SMP name badge used on the homepage

Keep editable or full-resolution originals outside the repository. Add new web-sized branding here and import it with `astro:assets` from the component that uses it.
